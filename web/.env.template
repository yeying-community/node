# Node API endpoint (optional).
# If set, API requests use this base; otherwise use same-origin `/api/*`.
# Also used to derive UCAN audience when VITE_UCAN_AUD is not set.
VITE_NODE_API_ENDPOINT=http://localhost:8100

# Audit approvers and required approvals are configured in Node `config.json`.

# UCAN capability settings (optional).
# If VITE_UCAN_AUD is not set, it is derived from VITE_NODE_API_ENDPOINT or window.location.
VITE_UCAN_AUD=did:web:localhost:8100
VITE_UCAN_RESOURCE=profile
VITE_UCAN_ACTION=read

# WebDAV settings for @yeying-community/web3-bs
# Required: base URL for WebDAV service.
VITE_WEBDAV_BASE_URL=https://webdav.example.com
# Optional: prefix path under WebDAV base.
VITE_WEBDAV_PREFIX=/webdav
# Optional: WebDAV audience override (default derived from BASE_URL).
VITE_WEBDAV_AUD=did:web:webdav.example.com
# Optional: public base for read access (fallback is BASE_URL + PREFIX).
VITE_WEBDAV_PUBLIC_BASE=https://cdn.example.com/webdav
# Optional: default avatar path or URL.
VITE_WEBDAV_AVATAR=/avatars/default.png
